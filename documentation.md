# מערכת לבנייה וסימולציה של שיחות קוליות אינטראקטיביות

## 1. סקירה כללית

מערכת זו מאפשרת למשתמשים לעצב, לבנות ולהריץ סימולציה של תהליכי שיחה מורכבים באמצעות ממשק גרפי אינטואיטיבי. המטרה המרכזית היא לספק פלטפורמה גמישה ליצירת סוכנים קוליים (Voice Agents) שמסוגלים לנהל דיאלוג עם משתמש קצה, להבין את כוונותיו, לחלץ מידע רלוונטי (כמו שמות, תאריכים ומספרי טלפון), ולקבל החלטות בהתאם ללוגיקה שהוגדרה.

הפרויקט בנוי כסימולטור המדמה שיחה בזמן אמת באמצעות המיקרופון והרמקולים של המחשב המקומי, אך הוא תוכנן מראש עם ארכיטקטורה מודולרית המאפשרת הסבה עתידית למערכת טלפוניה חיה המחוברת למרכזייה.

---

## 2. ארכיטקטורה

המערכת מורכבת משלושה רכיבים מרכזיים הפועלים יחד כדי לספק את החוויה המלאה:

### א. Frontend (צד לקוח)
- **טכנולוגיה:** React, React Flow, Tailwind CSS
- **מיקום:** תיקיית `client/`
- **תפקיד:**
  - לספק את ממשק המשתמש הגרפי (GUI) לבניית תהליכי השיחה.
  - המשתמש גורר ומשחרר "בלוקים" (Nodes) המייצגים פעולות שונות בשיחה (למשל, "דבר", "הקשב", "תנאי").
  - מאפשר לחבר בין הבלוקים כדי ליצור את הלוגיקה והזרימה של השיחה.
  - מנהל את התקשורת בזמן אמת עם ה-Backend באמצעות WebSocket כדי לשדר אירועים במהלך סימולציה (כמו איזה בלוק פעיל כרגע, מה המשתמש אמר, וכו').
  - מציג את תמלול השיחה ואת המידע שנאסף בזמן אמת.

### ב. Backend (צד שרת)
- **טכנולוגיה:** Node.js, Express, MongoDB, WebSocket (socket.io)
- **מיקום:** תיקיית `server/`
- **תפקיד:**
  - **ניהול נתונים:** אחראי על שמירה וטעינה של תהליכי השיחה (Flows) ממסד הנתונים MongoDB. כל "תרשים זרימה" שבנה המשתמש נשמר כמסמך JSON.
  - **נקודת כניסה (API):** מספק API מסוג REST עבור פעולות CRUD (Create, Read, Update, Delete) על תהליכי השיחה.
  - **תיווך ותזמור (Orchestration):** כאשר המשתמש מפעיל סימולציה, ה-Backend מקבל את הבקשה, טוען את נתוני השיחה, ומפעיל את רכיב הסימולטור (הסקריפט בפייתון) כתהליך בן (Child Process).
  - **תקשורת בזמן אמת:** פותח ערוץ WebSocket מול ה-Frontend. הוא מעביר את פלט הסימולטור (הודעות סטטוס, בקשות להשמעת קול, וכו') אל ה-Frontend, ומקבל מה-Frontend את קלט האודיו של המשתמש כדי להעבירו אל הסימולטור.

### ג. Simulator (הסימולטור)
- **טכנולוגיה:** Python
- **מיקום:** תיקיית `simulator/`
- **תפקיד:**
  - **"המוח" של המערכת:** זהו הרכיב שמריץ את לוגיקת השיחה בפועל, צעד אחר צעד, בהתאם לתרשים הזרימה שהוגדר.
  - **מנוע שיחה (Conversation Engine):** פועל כמכונת מצבים (State Machine) שעוברת בין הבלוקים בתרשים.
  - **זיהוי דיבור (Speech-to-Text - STT):** מקבל קלט אודיו מהמשתמש (דרך ה-Backend) ומתמלל אותו לטקסט באמצעות ספריית `openai-whisper`.
  - **הפקת דיבור (Text-to-Speech - TTS):** מקבל טקסט מהלוגיקה של השיחה וממיר אותו לקובץ שמע (MP3) באמצעות ספריית `edge-tts`.
  - **זיהוי ישויות (Named Entity Recognition - NER):** מנתח את הטקסט שתומלל כדי לזהות ולחלץ מידע ספציפי כמו שמות, תאריכים, מספרי טלפון וכו', באמצעות מודלי שפה מתקדמים מספריית `transformers`.
  - **תקשורת:** מתקשר עם ה-Backend דרך `stdin` (קלט סטנדרטי) ו-`stdout` (פלט סטנדרטי), כאשר כל ההודעות נשלחות בפורמט JSON.

---

## 3. מהלך שיחה (End-to-End)

כדי להבין כיצד הרכיבים עובדים יחד, הנה תיאור של זרימת המידע במהלך סימולציה טיפוסית:

1.  **הפעלה (Initiation):**
    *   המשתמש לוחץ על כפתור "Simulate" בממשק ה-Frontend.
    *   ה-Frontend שולח בקשת WebSocket ל-Backend עם מזהה תהליך השיחה (Flow ID).

2.  **אתחול (Initialization):**
    *   ה-Backend מקבל את הבקשה, טוען את נתוני השיחה המלאים מ-MongoDB.
    *   ה-Backend מפעיל את סקריפט הפייתון `simulator/main.py` כתהליך בן.
    *   נתוני השיחה המלאים (בפורמט JSON) מוזרמים ל-`stdin` של תהליך הפייתון.

3.  **ריצת השיחה (Execution Loop):**
    *   הסימולטור בפייתון קורא את מבנה השיחה ומתחיל לרוץ על הבלוקים, החל מבלוק ה-"Start".
    *   **כשהוא מגיע לבלוק "Speak":**
        *   הסימולטור מכין הודעת JSON מסוג `speak_start` הכוללת את הטקסט להשמעה.
        *   הודעה זו נדפסת ל-`stdout`.
        *   ה-Backend קורא את ההודעה ומעביר אותה מיד ל-Frontend דרך WebSocket.
        *   ה-Frontend מקבל את ההודעה, מציג את הטקסט על המסך, והסימולטור במקביל מייצר את קובץ השמע ומשמיע אותו.
    *   **כשהוא מגיע לבלוק "Listen":**
        *   הסימולטור שולח הודעת `status_update` עם המצב "listening".
        *   ה-Frontend מקבל את ההודעה ומפעיל את המיקרופון של המשתמש.
        *   כשהמשתמש מסיים לדבר, ה-Frontend שולח את גל הקול (Audio Blob) ל-Backend דרך WebSocket.
        *   ה-Backend מקבל את האודיו ומזרים אותו ישירות לסימולטור.
        *   הסימולטור משתמש ב-Whisper כדי לתמלל את האודיו לטקסט.
        *   לאחר התמלול, הוא מריץ את מודלי ה-NER כדי לחלץ ישויות מהטקסט, אם נדרש.

4.  **סיום (Termination):**
    *   הלולאה ממשיכה עד שהסימולטור מגיע לבלוק "End" או נתקל בשגיאה.
    *   הסימולטור שולח הודעת סיום, וה-Backend סוגר את תהליך הבן.
    *   ה-Frontend חוזר למצב עריכה.

---

## 4. המעבר למערכת טלפוניה אמיתית

זהו החלק המרכזי עבור הפיכת הסימולטור למערכת פרודקשן. המטרה היא להחליף את האינטראקציה המקומית (מיקרופון ורמקולים) בתקשורת מול מרכזיית טלפונים (PBX) או שירות טלפוניה בענן (כמו Twilio, Vonage, וכו').

הארכיטקטורה המודולרית מקלה על התהליך, מכיוון שרוב השינויים מתרכזים ברכיב ה-**Backend** ובאופן שבו הוא מתקשר עם ה-**Simulator**.

### א. שינויים נדרשים ב-Backend

ה-Backend יצטרך להפוך למתווך בין שירות הטלפוניה לבין הסימולטור.

1.  **קבלת שיחה נכנסת:**
    *   במקום לקבל הודעת WebSocket מה-Frontend כדי להתחיל סימולציה, ה-Backend יצטרך לחשוף נקודת קצה (Webhook) ששירות הטלפוניה יקרא אליה כאשר מתקבלת שיחה חדשה.
    *   כאשר שיחה נכנסת, ה-Backend יפעיל אירוע שיתחיל "סשן" חדש, יטען את ה-Flow המתאים, ויפעיל תהליך `simulator` חדש עבור אותה שיחה.

2.  **הזרמת אודיו דו-כיוונית:**
    *   **קלט (מהמתקשר למערכת):** שירות הטלפוניה ישלח את האודיו של המתקשר ל-Backend בזמן אמת, בדרך כלל דרך WebSocket. ה-Backend יצטרך לקבל את הזרם הזה ולהעביר אותו ל-`stdin` של תהליך הסימולטור הרלוונטי.
    *   **פלט (מהמערכת למתקשר):** כאשר הסימולטור מייצר אודיו (TTS), הוא כרגע משמיע אותו מקומית. יש לשנות את פונקציית `speak` בפייתון כך שבמקום להשמיע את הקובץ, היא תשדר אותו (או את הנתיב אליו) בחזרה ל-Backend. ה-Backend, בתורו, יצטרך להזרים את האודיו הזה בחזרה לשירות הטלפוניה כדי להשמיע אותו למתקשר.

### ב. שינויים נדרשים בסימולטור (Python - `simulator/main.py`)

השינויים המרכזיים בקוד הפייתון יתמקדו בשתי פונקציות:

1.  **`speak(text, language)`:**
    *   **היום:** הפונקציה משתמשת ב-`edge-tts` כדי ליצור קובץ MP3 זמני ומיד לאחר מכן משמיעה אותו באמצעות `playsound`.
    *   **השינוי הנדרש:** יש לשנות את הפונקציה כך שלא תנגן את הקובץ. במקום זאת, היא צריכה לשלוח הודעת JSON ל-`stdout` שתכיל את הנתיב לקובץ האודיו שנוצר.
        ```python
        # דוגמה לשינוי אפשרי
        def speak(text, language='en'):
            # ... (קוד יצירת קובץ ה-TTS נשאר זהה) ...

            # במקום לנגן, שלח הודעה ל-Backend
            send_message({
                "type": "play_audio_file",
                "path": temp_file_path,
                "duration": duration
            })

            # המתן עד שה-Backend יאשר שההשמעה הסתיימה
            # (זה ידרוש מנגנון קבלה של הודעות מה-Backend)
        ```
    *   ה-Backend יקבל את ההודעה, יקרא את קובץ האודיו, ויזרים אותו לשירות הטלפוניה.

2.  **`listen_for_command(model, language)`:**
    *   **היום:** הפונקציה משתמשת בספריית `speech_recognition` כדי להאזין למיקרופון המקומי.
    *   **השינוי הנדרש:** יש להסיר לחלוטין את השימוש ב-`speech_recognition`. במקום זאת, הפונקציה צריכה פשוט לקרוא את זרם האודיו הגולמי מה-`stdin`. ה-Backend יהיה אחראי להזרים את האודיו מהשיחה הנכנסת ישירות לתוך ה-`stdin` הזה.
        ```python
        # דוגמה לשינוי אפשרי
        def listen_for_command(model, language=None):
            send_message({"type": "status_update", "status": "listening"})

            # קרא את נתוני האודיו ישירות מה-stdin
            # (ה-Backend ידאג להזרים לכאן את המידע מהשיחה)
            audio_data = sys.stdin.buffer.read() # ייתכן וידרוש התאמות

            # ... (המשך הלוגיקה של תמלול באמצעות Whisper) ...
        ```

### ג. ניהול שיחות מרובות (Concurrency)

המודל הנוכחי של הפעלת תהליך פייתון נפרד לכל סימולציה מתאים היטב גם לניהול שיחות מרובות. כל שיחת טלפון נכנסת תפעיל `instance` נפרד של הסימולטור, מה שמבטיח בידוד מלא בין השיחות. ה-Backend יצטרך לנהל מפה (Map) של שיחות פעילות, כאשר כל מזהה שיחה (Session ID) ימופה למזהה התהליך (Process ID) של הסימולטור המתאים, כדי לנתב את הודעות האודיו והפיקוד בצורה נכונה.

---

## 5. דרישות טכניות לחיבור טלפוניה (SIP/RTP)

כדי לחבר את המערכת לרשת הטלפוניה הציבורית (PSTN), יש צורך להתממשק עם פרוטוקולים סטנדרטיים של טלפוניית IP. שני הפרוטוקולים המרכזיים לכך הם **SIP** ו-**RTP**.

### א. תפקידם של הפרוטוקולים

1.  **SIP (Session Initiation Protocol):**
    *   **תפקיד:** פרוטוקול איתותים (Signaling). הוא אחראי על ניהול מחזור החיים של השיחה.
    *   **פעולות עיקריות:**
        *   **הקמת שיחה:** שליחת בקשת `INVITE` ליעד כדי להתחיל שיחה.
        *   **ניהול השיחה:** משא ומתן על מאפייני המדיה (כמו סוג הקידוד לאודיו - Codec).
        *   **סיום שיחה:** שליחת הודעת `BYE` כדי לנתק את השיחה.
    *   במילים פשוטות, SIP הוא כמו ה"מרכזן" שאומר "שלום, אני רוצה להתחיל שיחה עם המספר הזה, האם אתה פנוי?".

2.  **RTP (Real-time Transport Protocol):**
    *   **תפקיד:** פרוטוקול תעבורה. הוא אחראי על העברת נתוני המדיה עצמם (במקרה שלנו, האודיו) בזמן אמת.
    *   **פעולות עיקריות:**
        *   **הזרמת אודיו:** לאחר ש-SIP הקים את השיחה, RTP מתחיל להזרים את חבילות (packets) השמע בין שני הצדדים.
        *   **סנכרון ותזמון:** RTP כולל מנגנונים למספור חבילות וחותמות זמן כדי לאפשר למקלט לסדר מחדש את החבילות שהגיעו לא לפי הסדר ולטפל בג'יטר (שינויים בעיכוב).
    *   במילים פשוטות, אחרי שהמרכזן (SIP) חיבר בין שני הצדדים, RTP הוא קו הטלפון עצמו שדרכו הקול עובר.

### ב. כיצד ה-Backend מתממשק לעולם ה-SIP

ה-Backend של המערכת לא יטפל ישירות בפרוטוקולי SIP/RTP הגולמיים. במקום זאת, הוא יתקשר עם **שרת SIP** (ידוע גם כ-SIP Proxy או PBX), שיעשה את העבודה הכבדה.

**אפשרויות לשרתי SIP:**
*   **שירותי ענן (מומלץ להתחלה):**
    *   **Twilio (SIP Trunking):** מאפשר לך לקבל מספר טלפון בענן. כאשר מישהו מתקשר למספר, Twilio יכול להעביר את השיחה ל-Backend שלך כאירוע SIP או דרך Webhook פשוט יותר. Twilio מנהל את כל רבדי ה-SIP/RTP המורכבים ומספק API נוח לעבודה.
    *   **Vonage, Plivo ודומיהם:** מציעים שירותים דומים.
*   **תוכנות קוד פתוח (להתקנה מקומית):**
    *   **Asterisk, FreeSWITCH:** מרכזיות טלפונים מבוססות תוכנה שניתן להתקין על שרת. הן מעניקות גמישות מלאה אך דורשות ידע וניהול בתחום הטלפוניה.

### ג. תרחיש לדוגמה עם שירות ענן (כמו Twilio)

1.  **קונפיגורציה:** אתה רוכש מספר טלפון ב-Twilio ומגדיר אותו כך שכל שיחה נכנסת תפעיל בקשת HTTP (Webhook) לנקודת קצה שתחשוף ב-Backend שלך (למשל, `POST /api/telephony/incoming-call`).
2.  **שיחה נכנסת:**
    *   לקוח מחייג למספר הטלפון.
    *   Twilio מקבל את השיחה ושולח בקשה ל-Webhook שלך עם פרטי השיחה.
3.  **ה-Backend בפעולה:**
    *   ה-Backend מקבל את הבקשה, מזהה שזו שיחה חדשה.
    *   הוא מפעיל תהליך `simulator` חדש ומעביר לו את מזהה השיחה.
    *   ה-Backend מגיב ל-Twilio עם הוראות ראשוניות, בדרך כלל בפורמט TwiML (שפת סימון של Twilio). ההוראה הראשונה יכולה להיות "התחל להזרים את האודיו ל-WebSocket".
4.  **הזרמת RTP דרך WebSocket:**
    *   Twilio פותח WebSocket מול ה-Backend ומתחיל לשלוח את האודיו של המתקשר בזמן אמת.
    *   ה-Backend מקבל את האודיו ומעביר אותו ל-`stdin` של תהליך הסימולטור המתאים.
5.  **הסימולטור מגיב:**
    *   הסימולטור מריץ את הלוגיקה, מגיע לבלוק "Speak", ומייצר קובץ אודיו.
    *   הוא שולח הודעה ל-Backend עם נתיב הקובץ.
6.  **השמעת התגובה:**
    *   ה-Backend קורא את קובץ האודיו ומזרים אותו בחזרה ל-Twilio (דרך ה-WebSocket או באמצעות הוראת TwiML נוספת).
    *   Twilio משמיע את האודיו למתקשר.
7.  **סיום השיחה:**
    *   כשהסימולטור מגיע לבלוק "End", הוא מודיע ל-Backend.
    *   ה-Backend שולח ל-Twilio הוראה לנתק את השיחה.

שילוב עם פלטפורמה כמו Twilio מפשט מאוד את התהליך, מכיוון שהוא חוסך את הצורך להתעסק ישירות עם המורכבות של ניהול רשתות SIP ו-RTP.
